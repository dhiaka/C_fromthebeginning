/*
9:41 PM
Thursday, February 4, 2021 (GMT+1)
Time in FOYER menzelABDERAHMEN , BIZERTE , TUNIS
instagram.com/dhiaakaa
 */

#include <stdio.h>
//  size of VARS ! c program #7
int main()
{
    int integerT;
    float realT;
    double doubleT;
    char caraT;
    
    printf("the size of int = %i bytes\n",sizeof(integerT));
    printf("the size of float = %i bytes\n",sizeof(realT));
    printf("the size of double = %i bytes\n",sizeof(doubleT));
    printf("the size of char = %i bytes",sizeof(caraT));
    
    return 0;
}
