/*
9:41 PM
Thursday, February 4, 2021 (GMT+1)
Time in FOYER menzelABDERAHMEN , BIZERTE , TUNIS
instagram.com/dhiaakaa
 */
 
#include <stdio.h>
// size of long vars  ! c program #8
int main()
{
    int a;
    long b;             //long = long int
    long long c;        //long long int 
    double d;
    long double e;
    
    printf("the size of int equals to %d \n",sizeof(a));
    printf("the size of long int equals to %d \n",sizeof(b));
    printf("the size of long long int equals to %d \n",sizeof(c));
    printf("the size of double equals to %d \n",sizeof(d));
    printf("the size of long double equals to %d \n",sizeof(e));
    
    return 0;
}
